# ------------------------------------------------------------------
# IMMICH CONFIGURATION
# Copy this file to .env and fill in your secure values.
# ------------------------------------------------------------------

# --- DATABASE CONNECTION (Server Side) ---
# Ezekkel az adatokkal lép be az Immich a Postgresbe
DB_HOSTNAME=database
DB_USERNAME=postgres
DB_PASSWORD=change_me_to_strong_password
DB_DATABASE_NAME=immich

# --- REDIS CONNECTION ---
REDIS_HOSTNAME=redis

# --- POSTGRES CONTAINER SETUP (Database Side) ---
# Ezek hozzák létre az adatbázist. 
# FONTOS: Ezeknek egyezniük kell a fenti DB_ adatokkal!
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me_to_strong_password
POSTGRES_DB=immich

# --- OPTIONAL / SYSTEM ---
# Időzóna beállítása (ajánlott)
TZ=Europe/Budapest