# ------------------------------------------------------------------
# NEXTCLOUD CONFIGURATION
# Copy this file to .env and update the values before running.
# ------------------------------------------------------------------

# --- DATABASE CONFIGURATION ---
# Ensure these match the values expected by the Postgres container
POSTGRES_USER=nextcloud
POSTGRES_PASSWORD=change_me_to_strong_password
POSTGRES_DB=nextcloud

# --- NEXTCLOUD INITIAL ADMIN USER (Optional) ---
# If set, Nextcloud will automatically create this admin account on first launch.
# You can remove these lines if you prefer to set it up via the Web UI.
NEXTCLOUD_ADMIN_USER=admin
NEXTCLOUD_ADMIN_PASSWORD=change_me_to_strong_password