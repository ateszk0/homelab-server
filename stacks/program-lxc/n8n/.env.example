# ------------------------------------------------------------------
# n8n AUTOMATION CONFIGURATION
# Copy this file to .env and update the values before running.
# ------------------------------------------------------------------

# --- DOMAIN SETTINGS ---
# The subdomain for n8n (e.g., "n8n")
N8N_SUBDOMAIN=n8n
# Your main domain (e.g., "example.com")
DOMAIN_NAME=example.com

# --- SYSTEM SETTINGS ---
TZ=Europe/Budapest

# --- SECURITY ---
# A random string to encrypt your credentials (API keys, passwords) in n8n.
# DO NOT LOSE THIS KEY, or you will lose access to your stored credentials!
# Generate one with: docker run --rm n8nio/n8n n8n encryption-key:generate
N8N_ENCRYPTION_KEY=change_me_to_a_random_string