# ------------------------------------------------------------------
# LOCAL LLM (OLLAMA + WEBUI) CONFIGURATION
# Copy this file to .env and update the values.
# ------------------------------------------------------------------

# --- OPEN WEBUI SECURITY ---
# A random string used to sign session cookies.
# Prevents getting logged out when the container restarts.
# Generate one with: openssl rand -base64 32
WEBUI_SECRET_KEY=change_me_to_a_random_string